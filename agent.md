# 项目维护规范

## 数据库表结构（强制）

- `backend/migrations/schema.sql` 是当前完整表结构快照，必须覆盖所有业务表及其字段、类型、默认值、NULL 约束、主键、唯一约束、检查约束、索引和表注释；尚未迁移移除的历史外键、函数及触发器也必须如实记录，不能只从快照中删去。
- 后续任何表结构变更（新增、修改、删除或重命名表、字段、约束、索引等）都必须在同一次改动中同步更新 `schema.sql`，不能只修改接口代码或新增迁移文件。
- 每张表必须有中文 `COMMENT ON TABLE`，说明业务用途、关键单位或历史兼容语义；新增表或用途变化时同步维护注释。
- 旧库升级通过新增编号迁移实现，不修改已执行的历史迁移。`000_initial_schema.sql` 为不可变历史基线；`schema.sql` 为完整快照，不用它代替旧库升级迁移。
- 涉及表注释的变更也应提供增量迁移，使已有数据库与快照一致。
- 修改结构后同步维护 `backend/migrations/verify.sql`、`backend/migrations/README.md`，执行 `rtk proxy bash -c 'bash "scripts/build-release-sql.sh" > "backend/migrations/release.sql"'` 重新生成上线 SQL；禁止直接编辑生成文件。
- 验证完整快照、编号迁移及生成上线 SQL 的最终结构一致，确保空库初始化和旧库升级兼容。`schema.sql` 不得包含业务数据、账号密码、密钥、测试记录或部署环境配置。
- 执行实际业务数据库的结构变更前须获得用户明确授权；维护 SQL 文件及隔离测试不等于授权线上执行。

## PostgreSQL 使用边界（强制）

- 不使用数据库外键（`FOREIGN KEY` / `REFERENCES`）及级联操作；关联字段仅保存关联 ID，关联对象的存在性、归属、有效状态及删除后的关系处理由应用层负责，并通过事务及必要的锁保证并发一致性。
- 不使用存储过程、自定义数据库函数、触发器等承载业务逻辑；业务校验、时间字段维护及软删除逻辑统一放在应用层，脚本和批量写入也必须遵守。
- 保留常规 SQL、事务、主键、索引、唯一约束、非空约束、检查约束、字段默认值及内置函数（如 `NOW()`）；禁止将上述约定扩大为取消金融幂等唯一约束或事务保护。
- 新增功能不得引入上述禁用特性。现有外键、自定义函数和触发器属于待迁移的历史实现，需先补齐应用层保障和回归测试，再通过新增编号迁移移除，并同步结构快照与核对脚本；不得改写历史迁移。本规范更新本身不代表已完成数据库改造。

## 时间字段与软删除（强制）

- 所有数据库业务表必须包含 `created_at`、`updated_at`、`deleted_at`，统一使用 PostgreSQL `TIMESTAMPTZ`。前两者非空、默认当前时间；`deleted_at` 可为空，NULL 表示未删除。
- 创建时间在后续更新中保持不变；所有更新（包括软删除）必须由应用层统一维护 `updated_at`，不新增或依赖时间维护触发器实现新功能。
- 所有业务删除一律通过 `UPDATE ... SET deleted_at = NOW(), updated_at = NOW() WHERE ... AND deleted_at IS NULL` 实现。禁止真实 `DELETE`、`TRUNCATE`、清空数据和物理级联删除，禁止为了方便重建记录而先真删旧数据；通过应用层统一删除入口、代码审查及回归测试约束，不新增防物理删除触发器。
- 列表、详情、统计、搜索、下拉选项、登录认证、关联校验及普通更新默认过滤 `deleted_at IS NULL`，不能读取或更新已删除业务对象。用户被软删除后，旧登录令牌及关联助手凭据必须失效。
- 业务名称、邮箱、地址、卡号指纹等可复用唯一键使用仅覆盖未删除记录的部分唯一索引，新增记录使用新 ID，不自动恢复或覆盖旧记录。验证码消费也必须软删除并保留历史，新验证码用新记录承载。
- 金融流水、支付幂等键及实际交易号唯一约束继续覆盖全部历史记录；新开通按邮箱和订阅周期防重，冲正可释放原订单周期但不得复用原实际交易号，不能通过软删除重复入账。只读对账和幂等检查允许明确读取删除记录，需在代码中说明审计用途；删除卡片保留余额及流水，禁止继续记账。
- 删除关联对象时明确处理业务关系，并在事务内执行；删除分组仅解除活跃账号绑定，不能删除账号。所有 UI 删除入口保留二次确认。
- 不新增自动清理历史业务数据或物理删除的运维脚本。软删除测试必须验证原行保留、普通查询隐藏已删除记录及更新时间正确；现有防物理删除触发器的过渡期测试仅可使用临时表，不得对业务库试删。涉及结构变更时同步完整 `schema.sql`、新增迁移和结构核对，并维护软删除测试。

## 充值与资金边界（强制）

- 客户收款、官网支出和会员开通核验分别持久化；只有核对了官网凭证、套餐和到期日才标记开通完成，不以浏览器启动、Session 导入或填表成功代替。
- 金额使用整数最小单位；钱包代币与银行卡 USD 余额独立。退款、冲正及余额调整必须保留原流水、新补偿流水、操作者和原因；订单、余额、流水和业务审计同事务提交。
- 部分退款按累计比例计算，重复请求必须校验幂等键与输入一致。渠道退款按退款 ID 合并，处理回调先于响应及重复/乱序通知，不凭重试创建第二笔退款。
- 不自动执行真实付款、退款或外部通知来验证代码；使用模拟渠道及隔离数据测试。结构测试不得对真实业务表删除或更新数据。

## 管理操作审计

- `/admin/*` 页面访问与用户操作复用 `operation_events`；新增管理路由须同步前后端审计路径白名单。身份以服务端认证为准，客户端不能指定操作者。
- 前端只上报固定动作枚举，禁止采集表单值、按钮原文、查询参数、剪贴板、密码、卡号和 Session。前端操作与本机执行结果明确标为“前端上报”，不能作为业务或资金成功证明。
- 后端记录变更、敏感读取及权限拒绝等执行结果；订单和资金业务审计与业务变更同事务提交，成功时避免重复通用记录。后台轮询与自动同步不制造操作噪音，上报接口本身不递归审计。
- 前端上报使用请求标识去重、有限重试，不持久化跨会话队列；审计列表仍受审计权限保护，时间展示使用 UTC+8。

## 套餐汇率

- 自动定价套餐以 PHP 原价为准，读取和下单时使用持久化汇率计算 USD 美分；汇率用十进制/有理数处理，最后一步四舍五入，不用浮点数计算后端金额。
- 每日同步汇率须保留来源、生效时间和同步历史，失败保留旧值并重试；过期汇率不得用于新自动定价订单。订单价格与所用汇率必须一并写入快照，后续同步不得改写历史订单、退款、钱包或卡片流水。
- PHP→USD、PHP→CNY 每天北京时间 09:00 同批同步并原子写入；只组合相同来源、生效时间和同步时间的币种，不混用不同批次。人民币价格为参考折算，自动定价按 PHP 原价直接换算，固定 USD 按同批交叉汇率换算，不改变订单结算币种。
- 套餐来源需区分 App Store 与网页版；仅导入用户确认的价格，不把参考汇率当作银行卡实际结算汇率。套餐导入脚本默认预览，结构迁移不包含业务价格数据。
- 用户确认的菲律宾 App Store 截图价格含税；本批导入按 12% VAT 以“含税价 ÷ 1.12”还原未税 PHP 原价，不能直接减去含税价的 12%。未税价舍入到 PHP 分后折算美元，名称注明未税，备注及订单快照保留含税原价、税率和去税依据。

## 角色与两步验证

- 管理员拥有所有业务模块权限；用户角色（含空角色）仅能查看和添加自己的账号，管理字段不下发。用户角色分配继续仅限环境超管。
- 管理员绑定验证器需校验当前登录密码并确认 TOTP；密钥只加密持久化，二维码在本机生成，禁止发送到外部服务或记录到审计。
- 本机浏览器凭据导出与后台浏览器启动均逐次验证 TOTP，验证器时间步事务消费、防并发重放并限流；普通用户不得调用这两个入口或使用旧助手授权。
